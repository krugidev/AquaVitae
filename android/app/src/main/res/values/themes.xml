<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Tema de plataforma mínimo (não usa a biblioteca com.google.android.material,
        que não é dependência deste projeto). Serve apenas de base para a Activity
        antes do Compose assumir o desenho da UI; a paleta/tipografia Material 3
        reais estão definidas em código em ui/theme/Theme.kt.
    -->
    <style name="Theme.AquaVitae" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/background</item>
        <item name="android:statusBarColor">@color/wine_primary</item>
    </style>
</resources>
