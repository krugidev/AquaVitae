# Os scripts de shell têm de ficar sempre com fim de linha LF: no Windows o autocrlf converte-os para CRLF no checkout e o
# Git Bash deixa de os conseguir correr ("bash\r: No such file or directory").
*.sh text eol=lf
